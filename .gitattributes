# Normalize line endings — store LF in the repo, check out native on Windows.
* text=auto eol=lf

# Treat these as binary (never normalize / diff as text)
*.db binary
*.png binary
*.jpg binary
*.ico binary
